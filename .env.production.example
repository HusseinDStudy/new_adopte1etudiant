# This is an example file for production environment variables.
# Copy this file to .env and fill in the values for your production environment.

# -------------------------------------
# Docker & Image Configuration
# -------------------------------------
# Your Docker Hub username is required to pull the correct images.
DOCKERHUB_USERNAME=your-dockerhub-username

# The following tags are typically set by the CI/CD pipeline.
# For manual deployment, you can set them to 'latest' or a specific git SHA.
API_IMAGE_TAG=latest
WEB_IMAGE_TAG=latest

# -------------------------------------
# Frontend Configuration
# -------------------------------------
# The API base URL for the frontend (should be /api in production)
VITE_API_URL=/api

# -------------------------------------
# Database Configuration
# -------------------------------------
# These values are used to configure the PostgreSQL container.
POSTGRES_USER=your-prod-db-user
POSTGRES_PASSWORD=your-secure-prod-db-password
POSTGRES_DB=your-prod-db-name

# This is the full connection string used by the API to connect to the database.
# Note that the host is 'postgres', which is the service name in docker-compose.prod.yml.
# This will be automatically constructed from the individual components below.
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public"

# -------------------------------------
# Application Secrets
# -------------------------------------
# A strong, random string used for signing JWTs. Generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret

# The public-facing URL of your web application.
WEB_APP_URL=https://your-domain.com

# -------------------------------------
# Google OAuth Credentials
# -------------------------------------
# These are obtained from the Google Cloud Console for your production application.
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-client-secret 