# Production Environment Configuration
# Copy this file to .env and fill in your production values

# Docker Hub Configuration
DOCKERHUB_USERNAME=your-dockerhub-username

# Database Configuration
DB_NAME=campusdb
DB_USER=postgres
DB_PASSWORD=your-secure-database-password-here

# JWT Configuration
# Generate a strong random string with: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-key-for-production

# Application URLs
WEB_APP_URL=https://your-domain.com
PORT=8080
NODE_ENV=production

# OAuth Providers
# Google OAuth Setup:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Create OAuth 2.0 credentials
# 5. Add your production domain to authorized origins
# 6. Add https://your-domain.com/api/auth/google/callback to authorized redirect URIs
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-client-secret



# Database Connection String (automatically generated from above values)
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}

# Note: In production, ensure you:
# 1. Use strong, unique passwords
# 2. Generate JWT secrets with proper entropy
# 3. Configure OAuth providers with production domains
# 4. Set up SSL/TLS certificates
# 5. Configure firewall rules
# 6. Set up regular database backups 