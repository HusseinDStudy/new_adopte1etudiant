# -------------------------------------
# Production Environment Variables
# -------------------------------------
# Copy this file to .env and fill in values for production.

# --- Docker & Images ---
DOCKERHUB_USERNAME=your-dockerhub-username
API_IMAGE_TAG=latest
WEB_IMAGE_TAG=latest

# --- Database ---
POSTGRES_USER=your-prod-db-user
POSTGRES_PASSWORD=your-secure-prod-db-password
POSTGRES_DB=your-prod-db-name
DB_HOST=postgres
DB_PORT=5432

# Derived – must come after DB vars above
# DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}?schema=public"
DATABASE_URL="postgresql://your-prod-db-user:your-secure-prod-db-password@postgres:5432/your-prod-db-name?schema=public"

# --- App URLs (public/base paths) ---
WEB_APP_URL=https://your-domain.com
VITE_API_URL=/api

# --- Auth & Security ---
JWT_SECRET=production-super-secure-jwt-secret-key

# --- Google OAuth ---
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-client-secret

# Server
NODE_ENV=production

# 
SLACK_WEBHOOK_URL