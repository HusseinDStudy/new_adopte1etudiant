# -------------------------------------
# Development Environment Variables
# -------------------------------------
# This file contains environment variables for local development.
# Copy this file to .env and modify the values as needed.

# -------------------------------------
# Database Configuration (for Docker Compose)
# -------------------------------------
# These values are used by both docker-compose.yml and docker-compose.db.yml
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=campusdb

# -------------------------------------
# Port Configuration (for Method 1: Full Docker)
# -------------------------------------
# These ports are used by docker-compose.yml for Method 1
DB_PORT=5432
API_PORT=8080
WEB_PORT=5173

# -------------------------------------
# API Configuration (for Method 1: Full Docker)
# -------------------------------------
# These are used by the API container in Method 1
JWT_SECRET=local-dev-jwt-secret-key
WEB_APP_URL=http://localhost:5173

# -------------------------------------
# Frontend Configuration (for both methods)
# -------------------------------------
# The API base URL for the frontend
VITE_API_URL=http://localhost:8080/api

# -------------------------------------
# Google OAuth Credentials (optional for development)
# -------------------------------------
# These are only needed if you want to test OAuth login
# Get these from Google Cloud Console
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
